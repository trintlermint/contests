
#include <iostream>
#include <vector>
using namespace std;

void testing() {
    int n; // sticks
    cin >> n;
    
    vector<int> ns(n);
    for (int i = 0; i < n; ++i)
    
    {
        cin >> ns[i];
    }
    
    // solution workflow
}

int main() {
    // NOTE: It can be proven that no symmetrical, non-degenerate convex polygon can be formed from any non-empty subset of the sticks.
    int t;
    cin >> t;
    
    while (t--) {
        testing();
    }
    
    return 0;
}
